[build]
buildCommand = "npm install && npx prisma generate && cd scraper && npm install"

[deploy]
startCommand = "cd scraper && npx tsx src/index.ts"

[[cron]]
schedule = "0 13 * * *"

[[cron]]
schedule = "0 4 * * *"
